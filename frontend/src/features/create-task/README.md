# Create Task feature

Modal form tạo task mới. Trigger từ Header "✚ Tạo Task".

Spec: [../../../../docs/features/create-task.md](../../../../docs/features/create-task.md)

## Files (sẽ build dần)
- `CreateTaskModal.tsx` — Dialog wrapper
- `TaskForm.tsx` — fields (title / assignee / sprint / status / priority / module / sp / deadline / desc)
